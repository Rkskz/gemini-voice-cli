import os
from dotenv import load_dotenv

# Initial setup for the voice assistant
def main():
    print("Gemini Voice CLI is starting...")
    # TODO: Initialize audio recording, STT, LLM, and TTS modules

if __name__ == "__main__":
    main()
